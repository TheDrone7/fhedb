[package]
name = "fhedb-core"
version = "0.1.0"
edition = "2024"

[dependencies]
fhedb-types = { path = "../fhedb-types" }
bson = { workspace = true, features = ["serde"] }
uuid = "1.22.0"
chrono = { workspace = true }

[dev-dependencies]
tempfile = "3.27.0"
