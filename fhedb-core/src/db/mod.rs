//! # Database
//!
//! This module contains the core database structures.

/// The database module - contains the Database struct.
pub mod database;

/// The collection module - contains the Collection struct.
pub mod collection;

/// The collection schema operations trait - contains traits for modifying collection schemas.
pub mod collection_schema_ops;

/// The schema module - contains the Schema struct.
pub mod schema;

/// The document module - contains the Document struct.
pub mod document;
